[package]
name = "ferrowasp-mspv2"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
heapless = { version = "0.9.2", features = ["serde"] }
postcard = { version = "1.1.3", default-features = false }
serde = { version = "1.0.228", default-features = false, features = ["derive"] }
