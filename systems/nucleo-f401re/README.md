# Nucleo-F401RE System

This is the first standalone system layout described in the
[FerroForge book](../../docs/src/prototype.md). `init/` owns native board setup,
`app_composition/` owns task selection and target/runtime choices, and
`gen_app/` is renderer output.

Run the documented commands from the repository root; do not hand-edit
`gen_app/`.
